/* ==========================================================================
   Infinite Heart — Play
   Intentionally left empty. This is the entry point PLAY on the main
   menu links to; the actual run logic (timer, hell rotation, scoring,
   etc.) will be wired in here later.
   ========================================================================== */

(function () {
  InfiniteHeartSubpage.initBackNavigation();

  // TODO: wire real game/run logic here.
})();
